# V2.11 Phase 0 Baseline (V2.10 Parity Safety Net)

## Purpose
Freeze a known-good pre-hard-cut baseline. Any failure here blocks legacy removal work.

---

## Parity Gate (Must Stay Green)

Command:

```bash
python -m pytest -q tests/test_run_api_contract.py tests/test_import_boundaries.py tests/behavioral_signatures
```

Latest run result:
- `pass` (`37 passed`)
- warnings only (report-template fallback warning for `acquisition`)

---

## Browser/API Lifecycle Invariant

Required lifecycle path:
- `PlanDraft -> PlanResolved -> RunComplete -> ReportComplete`

High-signal contract file:
- `tests/test_run_api_contract.py`

---

## Provenance Expectations (Pinned)

Required metadata fields at run/report boundaries:
- `plan_hash`
- `record_schema_version`
- `template_version_used`

These are enforced by API contract tests and must not regress during V2.11 hard-cuts.

---

## Phase 0 Exit Check

Before starting deletion/hard-cut phases:
1. Run parity gate command above.
2. Confirm all tests pass.
3. If parity fails, stop hard-cut work and resolve parity first.
