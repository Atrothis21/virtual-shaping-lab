# V3.22.15 Plan - Measurement Trace Promotion and Report Embedding

## Objective
Promote measurement outputs into first-class rollout/report artifacts with deterministic schema and compatibility-safe normalization.

---

## Entry Criteria
- V3.22.10 merged and green.
- Runtime measurement seam is active and post-run only.
- Protocol/policy trace promotion is stable.
- Report normalization contract tests are passing in mainline.

### Entry Criteria Status (Start of V3.22.15)
- V3.22.10 runtime measurement seam is present and exported:
  - `virtual_shaping_lab/vsl/runtime/measurement_adapter.py`
  - `virtual_shaping_lab/vsl/runtime/__init__.py`
- Post-run measurement integration surfaces are present:
  - `virtual_shaping_lab/vsl/rollout/replay_harness.py` (`run_with_measurement(...)`)
  - `virtual_shaping_lab/analysis/report/report.py` (runtime measurement embedding path)
- Protocol/policy trace promotion remains active in rollout record adapter:
  - `metadata.protocol_traces`
  - `metadata.policy_traces`
- Report normalization and record artifact paths remain active and stable in current mainline.

---

## Slices

## Slice 1 - Rollout Record Measurement Trace Promotion
### Scope
- Update:
  - `virtual_shaping_lab/vsl/records/adapters/rollout_records.py`
- Add stable fields:
  - `metadata.measurement_traces.metrics`
  - `metadata.measurement_traces.figures`
  - `metadata.measurement_traces.summary`
  - `metadata.measurement_traces.provenance`

### Implemented (this pass)
- Updated:
  - `virtual_shaping_lab/vsl/records/adapters/rollout_records.py`
- Added canonical rollout-record measurement trace extraction:
  - `_extract_measurement_traces(...)`
- Promoted canonical stable fields into rollout metadata on every adapted step:
  - `metadata.measurement_traces.metrics`
  - `metadata.measurement_traces.figures`
  - `metadata.measurement_traces.summary`
  - `metadata.measurement_traces.provenance`
- Deterministic behavior:
  - canonical trace object is always present (never omitted)
  - missing/non-object subfields normalize to deterministic empty defaults
  - populated values are normalized via stable-copy semantics

### Commit
- `feat(v3.22.15): promote measurement traces into rollout records`

## Slice 2 - Report Normalization for Measurement Outputs
### Scope
- Update:
  - `virtual_shaping_lab/analysis/report/report.py`
- Normalize measurement report-facing fields:
  - `measurement_metrics`
  - `measurement_figures`
  - `measurement_summary`
  - `measurement_provenance`

### Implemented (this pass)
- Updated:
  - `virtual_shaping_lab/analysis/report/report.py`
- Extended normalized report record defaults with canonical measurement-facing fields:
  - `measurement_metrics`
  - `measurement_figures`
  - `measurement_summary`
  - `measurement_provenance`
- Added canonical normalization path from rollout metadata:
  - source: `metadata.measurement_traces.*`
  - normalized outputs:
    - `measurement_metrics <- metadata.measurement_traces.metrics`
    - `measurement_figures <- metadata.measurement_traces.figures`
    - `measurement_summary <- metadata.measurement_traces.summary`
    - `measurement_provenance <- metadata.measurement_traces.provenance`
- Deterministic normalization behavior:
  - missing/non-conforming measurement trace subfields normalize to stable empty dict/list payloads

### Commit
- `feat(v3.22.15): normalize measurement trace fields in report outputs`

## Slice 3 - Compatibility Bridges and Expiry Markers
### Scope
- Add explicit bridge handling for legacy measurement payloads.
- Require owner + expiry metadata for deferred compatibility paths.
- Ensure deterministic precedence:
  - canonical `measurement_traces` wins over legacy fields.

### Implemented (this pass)
- Updated compatibility handling in:
  - `virtual_shaping_lab/analysis/report/report.py`
- Added explicit measurement compatibility bridge catalog with owner/expiry metadata:
  - `legacy_top_level_measurement_fields`
  - `legacy_metadata_measurement_payload`
  - `legacy_runtime_measurement_payload`
- Added deterministic bridge extraction and normalization helpers:
  - `_normalize_measurement_payload(...)`
  - `_extract_measurement_payload(...)`
- Enforced precedence contract:
  - canonical `metadata.measurement_traces` is authoritative when present
  - legacy bridges are applied only when canonical traces are absent
- Persisted bridge markers into normalized provenance when legacy paths are used:
  - `measurement_provenance.compatibility_bridges[*].{bridge, owner, expiry}`

### Commit
- `refactor(v3.22.15): add measurement compatibility bridges with expiry markers`

## Slice 4 - Schema and Normalization Guardrails
### Scope
- Add tests:
  - `tests/test_v3_measurement_rollout_record_schema.py`
  - `tests/test_v3_measurement_report_normalization.py`
  - `tests/test_v3_measurement_trace_compatibility_bridges.py`
- Enforce deterministic key ordering and payload stability.

### Implemented (this pass)
- Added rollout record schema guards:
  - `tests/test_v3_measurement_rollout_record_schema.py`
  - verifies canonical `metadata.measurement_traces` promotion into rollout records
  - verifies deterministic empty-default materialization when no measurement traces are supplied
- Added report normalization guards:
  - `tests/test_v3_measurement_report_normalization.py`
  - verifies canonical normalization into:
    - `measurement_metrics`
    - `measurement_figures`
    - `measurement_summary`
    - `measurement_provenance`
  - verifies deterministic empty defaults for missing measurement payloads
- Added compatibility bridge guards:
  - `tests/test_v3_measurement_trace_compatibility_bridges.py`
  - verifies precedence: canonical `measurement_traces` overrides legacy bridge inputs
  - verifies bridge marker emission with required owner/expiry metadata for legacy fallback paths

### Commit
- `test(v3.22.15): add measurement trace schema and normalization guardrails`

## Slice 5 - CI Bucket for Measurement Trace Promotion
### Scope
- Update:
  - `.github/workflows/ci.yml`
- Add blocking step:
  - `Run V3.22.15 measurement trace promotion`
- Include:
  - rollout schema promotion tests
  - report normalization tests
  - compatibility bridge tests

### Implemented (this pass)
- Updated:
  - `.github/workflows/ci.yml`
- Added blocking CI step:
  - `Run V3.22.15 measurement trace promotion`
- CI bucket now executes:
  - `tests/test_v3_measurement_rollout_record_schema.py`
  - `tests/test_v3_measurement_report_normalization.py`
  - `tests/test_v3_measurement_trace_compatibility_bridges.py`
- Enforcement intent:
  - fail on missing canonical measurement trace promotion in rollout records
  - fail on report normalization regressions for measurement-facing fields
  - fail on compatibility bridge precedence or owner/expiry marker regressions

### Commit
- `ci(v3.22.15): enforce measurement trace promotion contracts`

---

## Testing Gates
- `python -m pytest -q tests/test_v3_measurement_rollout_record_schema.py`
- `python -m pytest -q tests/test_v3_measurement_report_normalization.py`
- `python -m pytest -q tests/test_v3_measurement_trace_compatibility_bridges.py`

---

## CI Updates
- Add blocking bucket `Run V3.22.15 measurement trace promotion`.
- Bucket must fail on:
  - missing measurement trace fields in rollout/report outputs
  - non-deterministic measurement payload ordering
  - bridge-precedence regressions

---

## Exit Criteria
- Measurement outputs are first-class in rollout and report surfaces.
- Canonical measurement trace schema is deterministic and CI-guarded.
- Compatibility bridges are explicit, time-bounded, and non-ambiguous.
- No duplicate/ad-hoc measurement computation paths remain in report generation.
