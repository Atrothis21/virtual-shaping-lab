# V3.18.15 Plan - Legacy Deletion, CI Enforcement, and Single-Path Closeout

## Objective
Remove legacy learner-execution duplication and enforce one BOLT-faithful learner runtime path with hard CI protection.

---

## Entry Criteria
- V3.18.10 is merged and runtime adapter path is active.
- Executable presets RW/TD/PH/Mackintosh/TD-lambda are green.
- Record/report integration for learner internals is green.

### Entry Criteria Status (Start of V3.18.15)
- Runtime adapter path active: confirmed via `vsl/rollout/harness.py -> build_runtime_learner_adapter(...)`.
- Executable preset surface present: confirmed via `vsl/agent/learning/executable_presets.py`.
- Record/report learner trace propagation present: confirmed via rollout-record and report normalization adapters.

---

## Slices

## Slice 1 - Legacy Path Inventory and Ownership Matrix
### Scope
- Inventory all learner-execution branches outside canonical `vsl/agent/learning` + runtime adapter.
- Publish deletion matrix: keep, bridge, delete-now, delete-later.

### Implemented (this pass)
- Added:
  - `docs/v3_18_15_legacy_learner_path_inventory.md`
- Captured:
  - entry-criteria verification snapshot
  - learner-path ownership/deletion matrix
  - single-path closeout definition and downstream deletion notes

### Commit
- `docs(learning): publish legacy learner path inventory and deletion matrix`

## Slice 2 - Remove Duplicate Execution Branches
### Scope
- Delete duplicate learner execution logic in legacy assembly/runtime paths that bypass canonical bundle.
- Keep temporary adapters only where needed for backward compatibility.

### Implemented (this pass)
- Updated:
  - `virtual_shaping_lab/experiment/phases/learning_helpers.py`
  - `tests/test_learning_helpers.py`
  - `tests/test_v3_single_path_execution.py`
- Removed legacy fallback dispatch:
  - `agent.update(state, reward, action)`
- Enforced transition-only learner dispatch:
  - `agent.learn(Transition)`

### Commit
- `refactor(runtime): remove duplicate learner execution branches`

## Slice 3 - Remove Deprecated Surfaces
### Scope
- Remove stale import shims and obsolete modules superseded by canonical execution.
- Remove tracked `__pycache__` artifacts and enforce ignore policy.

### Implemented (this pass)
- Removed stale compatibility surface:
  - `virtual_shaping_lab/agents/learners/predictions/predictions.py`
- Tightened Python artifact ignore policy:
  - `.gitignore` now includes `**/__pycache__/` and `*.py[cod]`
- Verified no tracked `__pycache__`/`.pyc` files are present in index.

### Commit
- `chore(cleanup): remove deprecated learner surfaces and stale artifacts`

## Slice 4 - Hard CI Guardrails
### Scope
- Add CI checks:
  - banned import paths for runtime learner execution
  - no-new-legacy-branch assertions
  - single-path execution smoke tests
- Add drift tests for canonical learner contract hashing/snapshots.

### Implemented (this pass)
- Added:
  - `tests/test_v3_18_15_single_path_guardrails.py`
- Updated:
  - `.github/workflows/ci.yml`
- Guardrails now enforce:
  - no legacy learner-runtime import tokens in runtime surfaces
  - no reintroduction of legacy `update(state, reward, action)` dispatch path
  - single-path execution smoke coverage in blocking CI bucket
  - deterministic learner contract hash stability checks
  - API metadata identity checks post-cleanup

### Commit
- `ci(v3.18.15): enforce single learner execution path and import bans`

## Slice 5 - Closeout Documentation and PR Evidence
### Scope
- Publish final architecture note for V3.18 learner subsystem.
- Add PR evidence checklist with test-gate outputs and deleted-path inventory.

### Implemented (this pass)
- Added:
  - `docs/v3_18_15_single_path_learner_architecture.md`
  - `docs/v3_18_15_pr_evidence_checklist.md`
- Captured:
  - canonical single-path learner execution boundary
  - non-canonical/compatibility-only surfaces
  - PR checklist mapped to V3.18.15 guardrails and CI gates

### Commit
- `docs(v3.18): finalize single-path learner execution architecture and evidence`

---

## Testing Gates
- `python -m pytest -q tests/test_v3_runtime_learner_adapter.py tests/test_v3_single_path_execution.py`
- `python -m pytest -q tests/test_v3_namespace_import_audit.py tests/test_v3_namespace_hard_removal.py`
- `python -m pytest -q tests/test_v3_learner_runtime_parity.py tests/test_v3_learner_numeric_golden.py`
- `python -m pytest -q tests/test_run_api_contract.py -k "payload_mode_identity or basis_compile_identity or measurement_provenance_identity"`

---

## CI Updates
- Add blocking bucket: `Run V3.18.15 single-path enforcement`.
- Bucket must include:
  - banned-legacy-import audit
  - single learner execution path smoke tests
  - runtime parity + numeric golden checks
  - API metadata identity checks after cleanup

---

## PR and Commit Cadence
- 1 PR for V3.18.15, 5 commits (one per slice).
- All deletion commits must include explicit before/after test evidence in PR description.
- No fast-follow “fixup PR” for broken cleanup; PR must be green end-to-end before merge.

---

## Exit Criteria
- One learner execution path remains in runtime.
- Legacy duplicate execution modules/branches are removed or isolated as temporary adapters with expiry notes.
- CI blocks regressions back to multi-path learner execution.
- Documentation and test evidence prove BOLT-faithful single-path operation.
