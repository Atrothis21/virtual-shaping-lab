# V3.19.15 Plan - Single-Path Observation Closeout and CI Enforcement

## Objective
Remove duplicate/ad hoc observation construction paths and enforce one canonical runtime observation path with hard CI guardrails and closeout evidence.

---

## Entry Criteria
- V3.19.10 runtime observation adapter seam and record/report trace integration are merged and green.
- Observation parity tests confirm runtime adapter equivalence to direct bundle execution.
- Observation contract and executable buckets are stable in CI.

### Entry Criteria Status (Start of V3.19.15)
- V3.19.10 runtime observation seam is present in runtime code:
  - `vsl/runtime/observation_adapter.py`
  - `vsl/rollout/harness.py` calls runtime observation adapter before learner stepping
- Runtime observation parity/ownership tests exist:
  - `tests/test_v3_observation_runtime_parity.py`
  - `tests/test_v3_runtime_observation_adapter.py`
- Observation record/report trace integration is present:
  - `vsl/records/adapters/rollout_records.py` (`metadata.observation_traces`)
  - `analysis/report/report.py` observation trace normalization
- CI currently includes V3.19.10 observation runtime seam bucket in `.github/workflows/ci.yml`.

---

## Slices

## Slice 1 - Legacy Observation Path Inventory
### Scope
- Inventory all observation/feature construction outside canonical observation bundle + runtime adapter.
- Publish ownership/deletion matrix:
  - keep
  - bridge
  - delete-now
  - delete-later

### Implemented (this pass)
- Added legacy observation path inventory and ownership/deletion matrix:
  - `docs/v3_19_15_legacy_observation_path_inventory.md`
- Classified observation surfaces into:
  - `keep` (canonical runtime seam + record/report promotion paths)
  - `bridge` (temporary compatibility surfaces, with expiry notes)
  - `delete-now` (none safe without migration guardrails)
  - `delete-later` (phase-level direct `make_observation(...)` constructors + helper surface)
- Captured concrete Slice 2 follow-on targets and migration policy for removal sequencing.

### Commit
- `docs(observation): publish legacy observation path inventory and deletion matrix`

## Slice 2 - Remove Duplicate Observation Execution Branches
### Scope
- Delete direct feature-construction helpers in runtime/phase/agent paths that bypass observation adapter.
- Keep explicit temporary bridges only with expiry notes.

### Implemented (this pass)
- Removed runtime duplicate learner feature-construction branch in:
  - `virtual_shaping_lab/vsl/runtime/learner_adapter.py`
- Runtime learner adapter now requires canonical observation-feature inputs:
  - removed raw `stimulus`/`next_stimulus` execution path from `RuntimeLearnerAdapter.step(...)`
  - retained only:
    - `observation_features`
    - `observation_feature_names`
    - `next_observation_features`
    - `next_observation_feature_names`
- Updated runtime learner seam tests to enforce observation-feature-only stepping:
  - `tests/test_v3_runtime_learner_adapter.py`
- Legacy phase-level observation constructors remain explicitly bridged per Slice 1 inventory and are deferred to subsequent cleanup slices with expiry-driven removal sequencing.

### Commit
- `refactor(runtime): remove duplicate observation execution branches`

## Slice 3 - Remove Deprecated Observation Surfaces
### Scope
- Remove stale observation import shims and obsolete exports.
- Remove duplicate state constructors superseded by canonical observation output types.
- Enforce artifact hygiene (`__pycache__`, compiled artifacts).

### Implemented (this pass)
- Removed deprecated phase-level observation helper imports from active phase runtime surfaces:
  - `virtual_shaping_lab/experiment/phases/acquisition.py`
  - `virtual_shaping_lab/experiment/phases/compound_acquisition.py`
  - `virtual_shaping_lab/experiment/phases/compound_nonreinforcement.py`
  - `virtual_shaping_lab/experiment/phases/concurrent_schedule.py`
  - `virtual_shaping_lab/experiment/phases/criterion_shift.py`
  - `virtual_shaping_lab/experiment/phases/differential_acquisition.py`
  - `virtual_shaping_lab/experiment/phases/nonreinforcement.py`
  - `virtual_shaping_lab/experiment/phases/operant_acquisition.py`
  - `virtual_shaping_lab/experiment/phases/probe.py`
- Replaced direct helper-based construction with typed dataclass construction:
  - `Observation(...)` with explicit default-context normalization (`"A"` when phase context is `None`)
- Kept compatibility bridge surface for broader legacy/unit-test coverage:
  - `virtual_shaping_lab/agents/representations/observation.py::make_observation(...)`
  - bridge retained intentionally for non-phase legacy callers and will be handled under guardrailed closeout policy.
- Artifact hygiene verified as already enforced:
  - `.gitignore` contains `**/__pycache__/` and `*.py[cod]`; no additional artifact-ignore changes required in this slice.

### Commit
- `chore(cleanup): remove deprecated observation surfaces and stale artifacts`

## Slice 4 - Hard CI Guardrails and Drift Tests
### Scope
- Add blocking CI bucket:
  - banned observation legacy import tokens
  - no ad hoc observation-construction assertions
  - single-path runtime smoke tests
  - observation contract hash/snapshot drift checks

### Commit
- `ci(v3.19.15): enforce single observation execution path and import bans`

## Slice 5 - Closeout Documentation and PR Evidence
### Scope
- Publish final architecture note for observation subsystem.
- Add PR checklist requiring:
  - deleted-path inventory
  - before/after gate evidence
  - CI bucket proof

### Commit
- `docs(v3.19): finalize single-path observation architecture and evidence`

---

## Testing Gates
- `python -m pytest -q tests/test_v3_runtime_observation_adapter.py tests/test_v3_observation_runtime_parity.py tests/test_v3_single_path_observation_execution.py`
- `python -m pytest -q tests/test_v3_observation_namespace_import_audit.py tests/test_v3_observation_namespace_hard_removal.py`
- `python -m pytest -q tests/test_v3_observation_bundle_execution.py tests/test_v3_observation_golden.py`
- `python -m pytest -q tests/test_v3_rollout_record_schema.py tests/test_report.py -k "observation"`

---

## CI Updates
- Add blocking bucket: `Run V3.19.15 single-path observation enforcement`.
- Bucket must include:
  - observation legacy import-ban audit
  - single-path observation runtime smoke tests
  - observation runtime parity + golden checks
  - observation record/report trace checks

---

## PR and Commit Cadence
- 1 PR for V3.19.15, 5 commits (one per slice).
- No fixup PR for broken cleanup. PR must be green end-to-end.

---

## Exit Criteria
- One observation construction path remains in runtime:
  - `RuntimeObservationAdapter -> ObservationBundle.step(...)`
- Duplicate/ad hoc observation construction branches are removed or explicitly bridged with expiry notes.
- CI blocks regressions back to multi-path observation construction.
- Architecture note + checklist provide auditable closeout evidence.
