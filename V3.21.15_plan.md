# V3.21.15 Plan - Protocol Trace Promotion and Legacy Path Removal

## Objective
Promote protocol traces into rollout/report artifacts and remove duplicate legacy protocol execution paths from active runtime surfaces.

---

## Entry Criteria
- V3.21.10 merged and green.
- Runtime protocol seam active in harness.
- Protocol-agent boundary invariants passing.
- Legacy protocol path inventory from V3.21.0 available and current.

### Entry Criteria Status (Start of V3.21.15)
- V3.21.10 runtime protocol seam is present in active harness flow:
  - `virtual_shaping_lab/vsl/runtime/protocol_adapter.py`
  - `virtual_shaping_lab/vsl/rollout/harness.py`
- Protocol-agent boundary checks are present and tracked:
  - `tests/test_v3_agent_protocol_boundary_invariants.py`
  - `tests/test_v3_agent_protocol_loop_contract.py`
- Rollout record adapter schema lock and step-adapter gates are present:
  - `virtual_shaping_lab/vsl/records/adapters/rollout_records.py`
  - `tests/test_v3_rollout_record_schema.py`
- Legacy protocol-path ownership inventory remains available:
  - `docs/v3_21_0_legacy_protocol_path_inventory.md`

---

## Slices

## Slice 1 - Rollout Record Protocol Trace Promotion
### Scope
- Update:
  - `virtual_shaping_lab/vsl/records/adapters/rollout_records.py`
  - schema tests for protocol trace fields
- Add stable protocol fields:
  - `metadata.protocol_traces.emission`
  - `metadata.protocol_traces.consequence`
  - `metadata.protocol_traces.advance`
  - `metadata.protocol_traces.stop`
  - protocol provenance and timing counters.

### Implemented (this pass)
- Updated rollout record adapter:
  - `virtual_shaping_lab/vsl/records/adapters/rollout_records.py`
- Added protocol trace promotion in step adapter output:
  - `metadata.protocol_traces.emission`
  - `metadata.protocol_traces.consequence`
  - `metadata.protocol_traces.advance`
  - `metadata.protocol_traces.stop`
  - `metadata.protocol_traces.provenance.preset_name`
  - `metadata.protocol_traces.provenance.pipeline_order`
  - `metadata.protocol_traces.timing.t`
  - `metadata.protocol_traces.timing.phase_step`
  - `metadata.protocol_traces.timing.dt_s`
- Updated schema-focused rollout record tests:
  - `tests/test_v3_rollout_record_schema.py`
- Added dedicated gate:
  - `test_v3_rollout_step_adapter_promotes_protocol_traces_into_record_metadata`

### Commit
- `feat(v3.21.15): promote protocol traces into rollout records`

## Slice 2 - Report Normalization for Protocol Traces
### Scope
- Update:
  - `virtual_shaping_lab/analysis/report/report.py`
  - report contract tests
- Normalize protocol fields for report consumers:
  - emitted stimulus
  - outcome/consequence descriptors
  - timing/progression fields
  - protocol preset/provenance identifiers.

### Implemented (this pass)
- Updated report artifact normalization:
  - `virtual_shaping_lab/analysis/report/report.py`
- Added protocol-trace extraction and normalized report fields:
  - `protocol_emission`
  - `protocol_consequence`
  - `protocol_advance`
  - `protocol_stop`
  - `protocol_timing`
  - `protocol_provenance`
- Preserved compatibility behavior:
  - prefer `metadata.protocol_traces` when present
  - fallback to `metadata.protocol` for bridge-window inputs
- Updated report contract tests:
  - `tests/test_report.py`
- Added dedicated gate:
  - `test_normalize_record_for_artifact_promotes_protocol_traces`

### Commit
- `feat(v3.21.15): normalize protocol trace fields in report outputs`

## Slice 3 - Legacy Runtime Path Cleanup
### Scope
- Remove/bridge active runtime bypasses identified in inventory:
  - phase-local emission/consequence/advance/stop computations outside protocol seam
  - direct protocol logic duplicated in runtime loop helpers
- Add explicit bridge markers with owner + expiry where deletion is deferred.

### Commit
- `refactor(v3.21.15): remove duplicate runtime protocol paths`

## Slice 4 - Namespace and Import Hard Guards
### Scope
- Add:
  - `tests/test_v3_single_path_protocol_execution.py`
  - `tests/test_v3_protocol_namespace_import_audit.py`
  - `tests/test_v3_protocol_namespace_hard_removal.py`
- Enforce no regression to removed protocol namespaces/legacy imports.

### Commit
- `test(v3.21.15): add single-path protocol namespace guardrails`

## Slice 5 - CI Bucket for Trace + Single-Path Enforcement
### Scope
- Update:
  - `.github/workflows/ci.yml`
- Add blocking bucket:
  - `Run V3.21.15 protocol trace and single-path enforcement`
- Include:
  - protocol trace rollout/report checks
  - single-path protocol execution checks
  - namespace drift checks.

### Commit
- `ci(v3.21.15): enforce protocol trace promotion and single-path guards`

---

## Testing Gates
- `python -m pytest -q tests/test_v3_rollout_record_schema.py -k protocol`
- `python -m pytest -q tests/test_report.py -k protocol`
- `python -m pytest -q tests/test_v3_single_path_protocol_execution.py tests/test_v3_protocol_namespace_import_audit.py tests/test_v3_protocol_namespace_hard_removal.py`

---

## CI Updates
- Add blocking bucket `Run V3.21.15 protocol trace and single-path enforcement`.
- Bucket must fail on:
  - missing protocol trace promotion fields
  - runtime path bypass regressions
  - legacy namespace/import drift.

---

## Exit Criteria
- Protocol traces are first-class in rollout and report normalized outputs.
- Active runtime protocol flow has one execution path.
- Legacy protocol bypass paths are removed or explicitly bridged with expiry metadata.
- Namespace and import guardrails prevent regression.
