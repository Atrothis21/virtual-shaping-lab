# V3.22.10 Plan - Runtime Measurement Seam and Boundary Enforcement

## Objective
Introduce a canonical runtime measurement adapter seam and enforce strict causal separation:
measurement consumes records after execution and never drives protocol/agent control flow.

---

## Entry Criteria
- V3.22.5 merged and green.
- Executable measurement bundle + presets are deterministic and test-covered.
- Runtime protocol and agent seams remain single-path and CI-guarded.
- Record schema/traces (`policy_traces`, `protocol_traces`) are stable.

### Entry Criteria Status (Start of V3.22.10)
- V3.22.5 executable measurement core surfaces are present:
  - `virtual_shaping_lab/vsl/measurement/operators/*`
  - `virtual_shaping_lab/vsl/measurement/bundle.py`
  - `virtual_shaping_lab/vsl/measurement/executable_presets.py`
- Measurement executable coverage is present:
  - `tests/test_v3_measurement_operators_base.py`
  - `tests/test_v3_measurement_operators_analysis.py`
  - `tests/test_v3_measurement_operators_visualization.py`
  - `tests/test_v3_measurement_bundle_execution.py`
  - `tests/test_v3_measurement_executable_instantiation.py`
  - `tests/test_v3_measurement_golden.py`
- Runtime protocol/agent seams remain exported and single-path guarded:
  - `virtual_shaping_lab/vsl/runtime/protocol_adapter.py`
  - `virtual_shaping_lab/vsl/agent/composite.py`
  - `virtual_shaping_lab/vsl/rollout/harness.py`
- Rollout/report record boundaries continue to expose trace surfaces needed by measurement:
  - `policy_traces`
  - `protocol_traces`

---

## Slices

## Slice 1 - Runtime Measurement Adapter
### Scope
- Add:
  - `virtual_shaping_lab/vsl/runtime/measurement_adapter.py`
- Define:
  - `RuntimeMeasurementAdapter`
  - `build_runtime_measurement_adapter(...)`
- Require adapter input surface:
  - list of normalized rollout records only

### Implemented (this pass)
- Added canonical runtime measurement seam:
  - `virtual_shaping_lab/vsl/runtime/measurement_adapter.py`
  - `RuntimeMeasurementAdapter`
  - `build_runtime_measurement_adapter(...)`
- Enforced adapter input boundary:
  - accepts only `list` of mapping-like normalized rollout records
  - fails fast on non-list or non-mapping record entries
- Routed runtime measurement execution through executable measurement bundle seam:
  - `build_executable_measurement_preset(...)`
  - `ExecutableMeasurementPreset.bundle.step(...)`
- Added runtime metadata envelope for provenance:
  - `runtime_measurement.preset_name`
  - `runtime_measurement.normalization`
- Updated runtime and public exports:
  - `virtual_shaping_lab/vsl/runtime/__init__.py`
  - `virtual_shaping_lab/vsl/__init__.py`

### Commit
- `feat(v3.22.10): add runtime measurement adapter seam`

## Slice 2 - Post-Run Integration Surface
### Scope
- Update:
  - measurement invocation path in report/replay integration surfaces
- Ensure measurement invocation is post-rollout only:
  - after `RolloutHarness` / `ReplayHarness` completion
- Disallow per-step runtime measurement dispatch.

### Implemented (this pass)
- Updated replay integration to expose explicit post-run measurement seam invocation:
  - `virtual_shaping_lab/vsl/rollout/replay_harness.py`
  - added `ReplayHarness.run_with_measurement(...)`
  - runs rollout first, then executes `RuntimeMeasurementAdapter.step(...)` over emitted records
- Added deterministic replay-record normalization for runtime measurement:
  - `_records_to_runtime_measurement_payload(...)`
  - preserves post-run read-only boundary from rollout records into measurement input
- Updated report integration to invoke runtime measurement seam post-run:
  - `virtual_shaping_lab/analysis/report/report.py`
  - added runtime measurement preset resolution and record normalization helpers
  - invokes `build_runtime_measurement_adapter(...).step(...)` after records are finalized
- Persisted post-run runtime measurement artifact for integration evidence:
  - `runtime_measurement.json`
- Preserved causal/runtime boundary:
  - no measurement dispatch added to environment or step-loop paths
  - measurement remains post-rollout/report-consumer only

### Commit
- `refactor(v3.22.10): route measurement execution through post-run runtime seam`

## Slice 3 - Boundary Invariants and Guards
### Scope
- Add tests:
  - `tests/test_v3_measurement_runtime_boundary_invariants.py`
  - `tests/test_v3_measurement_runtime_loop_contract.py`
- Enforce:
  - no measurement writes to agent/protocol runtime state
  - no measurement invocation inside step loop
  - no protocol-side learner/internal measurement shortcuts

### Implemented (this pass)
- Added runtime measurement boundary invariant coverage:
  - `tests/test_v3_measurement_runtime_boundary_invariants.py`
- Guarded read-only measurement behavior:
  - runtime measurement adapter does not mutate caller-provided record payloads
  - replay integration executes measurement through post-run seam (`run_with_measurement(...)`)
- Added runtime loop contract guards:
  - `tests/test_v3_measurement_runtime_loop_contract.py`
  - asserts no runtime measurement dispatch in `CompiledProgramTestEnvironment.step(...)`
  - asserts replay harness dispatch order remains:
    - rollout completion first
    - measurement seam invocation second
- Added source-level boundary checks preventing agent/protocol mutator leakage into runtime measurement seam
  - forbidden tokens include learner/policy/protocol step-mutator APIs in runtime measurement adapter

### Commit
- `test(v3.22.10): add measurement runtime boundary invariants`

## Slice 4 - Runtime Parity and Determinism
### Scope
- Add tests:
  - `tests/test_v3_runtime_measurement_adapter.py`
  - `tests/test_v3_measurement_runtime_parity.py`
- Prove parity:
  - runtime adapter output == direct measurement bundle output
- Prove deterministic hash identity under fixed records + seed.

### Implemented (this pass)
- Added runtime measurement adapter contract tests:
  - `tests/test_v3_runtime_measurement_adapter.py`
  - validates runtime metadata envelope and canonical measurement pipeline markers
  - validates deterministic hash identity for repeated adapter execution over fixed normalized records
- Added runtime parity tests:
  - `tests/test_v3_measurement_runtime_parity.py`
  - proves runtime adapter output parity with direct executable measurement bundle invocation
  - proves deterministic runtime output on replay-produced records under fixed seed
- Parity and determinism checks are scoped to post-run measurement records only
  - no per-step coupling introduced into protocol/agent runtime loop

### Commit
- `test(v3.22.10): add runtime measurement parity and determinism gates`

## Slice 5 - CI Bucket for Runtime Measurement Seam
### Scope
- Update:
  - `.github/workflows/ci.yml`
- Add blocking step:
  - `Run V3.22.10 runtime measurement seam`
- Include:
  - runtime adapter/parity
  - boundary invariants
  - determinism checks

### Commit
- `ci(v3.22.10): enforce runtime measurement seam contracts`

---

## Testing Gates
- `python -m pytest -q tests/test_v3_runtime_measurement_adapter.py tests/test_v3_measurement_runtime_parity.py`
- `python -m pytest -q tests/test_v3_measurement_runtime_boundary_invariants.py tests/test_v3_measurement_runtime_loop_contract.py`
- `python -m pytest -q tests/test_v3_rollout_replay_harness.py -k "measurement or replay or hash"`

---

## CI Updates
- Add blocking bucket `Run V3.22.10 runtime measurement seam`.
- Bucket must fail on:
  - measurement seam bypass
  - runtime loop coupling regressions
  - non-deterministic measurement outputs

---

## Exit Criteria
- Measurement execution has one canonical runtime seam (`RuntimeMeasurementAdapter`).
- Measurement remains post-run and read-only with respect to protocol/agent runtime state.
- Runtime adapter parity + determinism are test-backed and CI-enforced.
- Causal boundary constraints from `agent_protocol_interaction.md` are explicitly guarded.
