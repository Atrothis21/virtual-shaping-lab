# V3.18.5 Plan - Executable Core Learner Bundle (P, Delta, W)

## Objective
Deliver the first executable learner core on top of canonical contracts: concrete `P`, `Delta`, `W` operators and `LearnerBundle.step()` with deterministic numeric validation.

---

## Entry Criteria
- V3.18.0 is complete (canonical spec, state naming, instantiation boundary).
- `vsl/agent/learning` has no unresolved spec ownership conflicts.
- Existing RW/TD learner behavior in legacy path is available for parity references.

---

## Slices

## Slice 1 - Operator Protocols and Null Objects
### Scope
- Add executable operator protocols in `vsl/agent/learning/operators/base.py`:
  - `PredictionOperator`
  - `ErrorOperator`
  - `UpdateOperator`
  - optional `AttentionOperator`, `EligibilityOperator`
- Add null objects for optional operators.

### Commit
- `feat(learning): add executable operator protocols and null optional operators`

## Slice 2 - Prediction Operators
### Scope
- Implement:
  - linear state-value prediction
  - tabular state-value prediction
  - linear action-value prediction (for future operant path)
- Enforce one prediction output contract.

### Commit
- `feat(learning): implement prediction operators (linear/tabular/action-value)`

## Slice 3 - Error + Update Operators
### Scope
- Implement:
  - RW error
  - TD(0) error
  - RW update
  - TD(0) update
- Keep update mutation ownership strictly within update operator.

### Commit
- `feat(learning): implement RW/TD error and update operators`

## Slice 4 - LearnerBundle Execution Core
### Scope
- Add `vsl/agent/learning/bundle.py` with canonical `step()` order:
  1. predict
  2. error
  3. optional attention/eligibility hooks (null by default)
  4. update
- Persist intermediate outputs needed for measurement.

### Commit
- `feat(learning): add executable learner bundle step orchestration`

## Slice 5 - Core Presets and Numeric Proof
### Scope
- Materialize executable presets:
  - `rescorla_wagner`
  - `td0`
- Add golden numeric fixtures and trajectory tests for acquisition/extinction/TD propagation.

### Commit
- `test(learning): add executable preset parity and golden numeric tests`

---

## Testing Gates
- `python -m pytest -q tests/test_v3_learner_operators_base.py tests/test_v3_learner_operators_prediction.py tests/test_v3_learner_operators_error.py tests/test_v3_learner_operators_update.py`
- `python -m pytest -q tests/test_v3_learner_bundle_execution.py`
- `python -m pytest -q tests/test_v3_learner_numeric_golden.py`
- `python -m pytest -q tests/test_v3_learner_executable_instantiation.py`
- `python -m pytest -q tests/test_v3_learner_presets.py tests/test_v3_learner_runtime_parity.py -k "executable or runtime_acceptance_parity_matches_registry_matrix"`

---

## CI Updates
- Add blocking bucket: `Run V3.18.5 executable learner core`.
- Bucket must include:
  - operator contract tests
  - bundle execution tests
  - numeric golden fixtures
  - preset materialization parity

---

## PR and Commit Cadence
- 1 PR for V3.18.5, 5 commits (one per slice).
- Commit each slice only after local slice gate passes.
- Keep PR focused on P/Delta/W core only; defer A/E behavior to next plan.

---

## Exit Criteria
- Legal symbolic specs instantiate to executable operator objects.
- `LearnerBundle.step()` executes deterministic RW and TD(0) paths.
- Numeric trajectory tests pass for acquisition/extinction/TD propagation.
- No preset-specific branching inside bundle orchestration.

### Completion Pass Notes
- Added symbolic-spec to executable bundle materialization for supported V3.18.5 tuples (`RW`, `TD0`).
- Added dedicated executable-instantiation tests to enforce legal-spec mapping behavior and unsupported-spec diagnostics.
- Added blocking CI bucket `Run V3.18.5 executable learner core` with focused operator/bundle/golden/parity coverage.
