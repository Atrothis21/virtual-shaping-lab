# V3.19.0 Plan - Observation Contract Ownership and Boundary Hardening

## Objective
Establish one canonical observation composition contract (`Φ`, `C`, `G`) with explicit typed outputs, legality-first materialization, and CI ownership guardrails.

---

## Entry Criteria
- V3.18.15 single-path learner execution is merged and green.
- Runtime learner seam (`RuntimeLearnerAdapter -> LearnerBundle.step`) is active.
- No observation executable bundle exists yet in runtime.
- Team agreement that observation semantics are owned by VSL observation contracts, not runtime helper transport code.

### Entry Criteria Status (Start of V3.19.0)
- V3.18.15 single-path learner execution: confirmed via `experiment/phases/learning_helpers.py` transition-only dispatch and V3.18.15 guardrail coverage.
- Runtime learner seam active: confirmed via `vsl/runtime/learner_adapter.py` and rollout harness adapter usage.
- Observation executable bundle absent: confirmed no `vsl/agent/observation/instantiate.py` or runtime-bound observation bundle surface exists yet.
- Observation ownership boundary ready: canonical ownership scaffold added under `vsl/agent/observation/*` in this slice.

---

## Slices

## Slice 1 - Canonical Observation Spec Ownership
### Scope
- Add canonical observation symbolic spec owner:
  - `vsl/agent/observation/spec.py`
- Add legality rules for symbolic tuple axes:
  - representation (`Φ`)
  - context (`C`)
  - generalization (`G`)
- Add registry/preset scaffolding and validation entrypoints.

### Implemented (this pass)
- Added canonical observation contract package:
  - `virtual_shaping_lab/vsl/agent/observation/spec.py`
  - `virtual_shaping_lab/vsl/agent/observation/validation.py`
  - `virtual_shaping_lab/vsl/agent/observation/registry.py`
  - `virtual_shaping_lab/vsl/agent/observation/presets.py`
  - `virtual_shaping_lab/vsl/agent/observation/__init__.py`
- Exported observation contract surface through package facades:
  - `virtual_shaping_lab/vsl/agent/__init__.py`
  - `virtual_shaping_lab/vsl/__init__.py`
- Added slice-1 contract tests:
  - `tests/test_v3_observation_grammar_spec.py`
  - `tests/test_v3_observation_validator.py`
  - `tests/test_v3_observation_registry.py`
  - `tests/test_v3_observation_presets.py`

### Commit
- `feat(observation): add canonical observation grammar and legality surface`

## Slice 2 - Typed Observation Output Contract
### Scope
- Introduce typed observation artifact:
  - `ObservationOutput`
- Fields:
  - `raw_stimulus`
  - `representation`
  - `context_state`
  - `generalized_state`
  - `features`
  - `feature_names`
  - `metadata`
- Add compatibility normalization for one-release legacy naming drift.

### Implemented (this pass)
- Added typed observation output contract:
  - `virtual_shaping_lab/vsl/agent/observation/output.py`
- Added one-release compatibility normalization aliases:
  - `raw_observation -> raw_stimulus`
  - `state_representation -> representation`
  - `context -> context_state`
  - `generalized -> generalized_state`
  - `feature_vector -> features`
  - `feature_labels -> feature_names`
- Exported output contract surface through package facades:
  - `virtual_shaping_lab/vsl/agent/observation/__init__.py`
  - `virtual_shaping_lab/vsl/agent/__init__.py`
  - `virtual_shaping_lab/vsl/__init__.py`
- Added slice-2 contract tests:
  - `tests/test_v3_observation_output_contract.py`

### Commit
- `feat(observation): add typed observation output contract and normalization`

## Slice 3 - Legality-First Materialization Boundary
### Scope
- Add `instantiate.py` for symbolic tuple -> typed materialization artifacts.
- Add `ObservationInstantiationArtifact`.
- Add failure catalog/codes for invalid tuple materialization.
- Enforce legality before any operator instantiation.

### Commit
- `feat(observation): add legality-first observation instantiation boundary`

## Slice 4 - Ownership/Namespace Guardrails
### Scope
- Add ownership audit tests for observation spec/runtime transport boundaries.
- Add namespace hard-removal/anti-shadow import checks for observation internals.
- Add CI bucket for V3.19.0 ownership hardening.

### Commit
- `ci(v3.19.0): enforce observation contract ownership and namespace guardrails`

---

## Testing Gates
- `python -m pytest -q tests/test_v3_observation_grammar_spec.py tests/test_v3_observation_validator.py tests/test_v3_observation_registry.py tests/test_v3_observation_presets.py`
- `python -m pytest -q tests/test_v3_observation_spec_adapters.py tests/test_v3_observation_instantiation.py`
- `python -m pytest -q tests/test_v3_observation_contract_ownership.py tests/test_v3_namespace_import_audit.py tests/test_v3_namespace_hard_removal.py`

---

## CI Updates
- Add blocking bucket: `Run V3.19.0 observation contract ownership`.
- Bucket must include:
  - observation grammar/validator/registry/preset tests
  - observation spec adapter + instantiation tests
  - ownership + namespace drift tests

---

## PR and Commit Cadence
- 1 PR for V3.19.0, 4 commits (one per slice).
- PR must include ownership note documenting canonical observation semantics boundary.

---

## Exit Criteria
- One canonical symbolic observation composition owner exists and is exported.
- Typed `ObservationOutput` contract exists and is serialization-tested.
- Observation materialization is legality-first with failure catalog coverage.
- CI blocks namespace/ownership drift for observation contract surfaces.
