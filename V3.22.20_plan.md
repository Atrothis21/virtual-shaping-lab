# V3.22.20 Plan - Measurement Closeout, CI Hardening, and Evidence

## Objective
Complete V3.22 closeout with end-to-end measurement integration, replay/hash determinism, architecture documentation, and PR evidence standards.

---

## Entry Criteria
- V3.22.15 merged and green.
- Measurement grammar, executable core, runtime seam, and trace promotion are active.
- No open blocker in replay/hash stability for measurement-inclusive runs.
- All prior V3.22 CI buckets are green in mainline configuration.

### Entry Criteria Status (Start of V3.22.20)
- V3.22 measurement ownership + executable core surfaces are present:
  - `virtual_shaping_lab/vsl/measurement/spec.py`
  - `virtual_shaping_lab/vsl/measurement/bundle.py`
  - `virtual_shaping_lab/vsl/measurement/executable_presets.py`
- Runtime measurement seam and post-run integration are active:
  - `virtual_shaping_lab/vsl/runtime/measurement_adapter.py`
  - `virtual_shaping_lab/vsl/rollout/replay_harness.py` (`run_with_measurement(...)`)
  - `virtual_shaping_lab/analysis/report/report.py` (runtime measurement embedding)
- Measurement trace promotion + report normalization surfaces are present:
  - `virtual_shaping_lab/vsl/records/adapters/rollout_records.py` (`metadata.measurement_traces`)
  - `virtual_shaping_lab/analysis/report/report.py` (`measurement_metrics|figures|summary|provenance`)
- V3.22 CI buckets for ownership/runtime seam/trace promotion are already defined in workflow.

---

## Slices

## Slice 1 - End-to-End Measurement Integration Matrix
### Scope
- Add matrix tests spanning:
  - protocol + observation + learner + policy + measurement + report
- Cover classical and actioned protocol families across shared measurement presets.
- Validate phenomenon-signature mapping coverage from `behavior_measurement.md`.

### Implemented (this pass)
- Added:
  - `tests/test_v3_measurement_integration_matrix.py`
- Added end-to-end integration matrix coverage across:
  - protocol/runtime stepping (`CompiledProgramTestEnvironment`, `RolloutHarness`)
  - observation/learner/policy runtime metadata surfaces
  - post-run measurement execution seam (`ReplayHarness.run_with_measurement(...)`)
  - report normalization path (`_normalize_record_for_artifact(...)`)
- Matrix currently covers:
  - classical family: `acquisition` + `learning_curve_basic`
  - actioned family: `operant_conditioning` + `action_learning_curve`
- Added phenomenon-signature mapping assertions aligned to `behavior_measurement.md` by asserting preset materialization and expected metric signatures per family

### Commit
- `test(v3.22.20): add end-to-end measurement integration matrix`

## Slice 2 - Determinism and Replay Hardening (Measurement-Inclusive)
### Scope
- Extend replay/hash tests with measurement traces embedded.
- Add/extend tests around:
  - repeated seeded replay hash identity
  - stable measurement trace ordering and payload hashing
  - deterministic figure/metric artifact identity references

### Implemented (this pass)
- Updated measurement-inclusive replay hardening in:
  - `virtual_shaping_lab/vsl/rollout/replay_harness.py`
- Added deterministic measurement trace embedding into replay records from post-run measurement output:
  - `metadata.measurement_traces.metrics`
  - `metadata.measurement_traces.figures`
  - `metadata.measurement_traces.summary`
  - `metadata.measurement_traces.provenance`
- Added deterministic measurement artifact identity references per replay record:
  - `metadata.measurement_artifact_identity.hash_algorithm`
  - `metadata.measurement_artifact_identity.measurement_payload_hash`
  - `metadata.measurement_artifact_identity.preset_name`
- Added stable payload hashing helper for measurement-inclusive replay determinism:
  - `_measurement_payload_hash(...)`
- Extended replay/hash tests:
  - `tests/test_v3_rollout_replay_harness.py`
  - added repeated-run hash identity assertions for `run_with_measurement(...)`
  - added deterministic measurement trace/identity reference assertions

### Commit
- `fix(v3.22.20): harden measurement replay determinism and hash stability`

## Slice 3 - Final CI Aggregation Bucket
### Scope
- Update:
  - `.github/workflows/ci.yml`
- Add blocking step:
  - `Run V3.22 measurement closeout gates`
- Aggregate required buckets:
  - measurement ownership
  - executable measurement core
  - runtime measurement seam/parity
  - trace promotion/report normalization
  - matrix + replay determinism

### Implemented (this pass)
- Updated:
  - `.github/workflows/ci.yml`
- Added blocking closeout step:
  - `Run V3.22 measurement closeout gates`
- Aggregated V3.22 measurement closeout coverage in one enforcement bucket:
  - measurement ownership + grammar/validator/registry/presets
  - executable measurement operator/bundle/preset/golden core
  - runtime measurement seam + parity + boundary invariants
  - trace promotion/report normalization/compatibility bridge guards
  - end-to-end matrix integration and replay/hash determinism checks
- Closeout bucket fails on:
  - ownership/runtime seam drift
  - trace normalization/bridge regressions
  - measurement-inclusive replay/hash non-determinism
  - cross-subsystem integration regressions

### Commit
- `ci(v3.22.20): add measurement closeout enforcement bucket`

## Slice 4 - Architecture and Evidence Documentation
### Scope
- Add:
  - `docs/v3_22_measurement_architecture.md`
  - `docs/v3_22_pr_evidence_checklist.md`
- Document:
  - measurement subsystem ownership and seam boundaries
  - banned/non-canonical measurement paths
  - required evidence for future measurement changes

### Implemented (this pass)
- Added:
  - `docs/v3_22_measurement_architecture.md`
  - `docs/v3_22_pr_evidence_checklist.md`
- Documented canonical V3.22 measurement seam and ownership split:
  - runtime protocol/agent owns in-band stepping and record emission
  - measurement owns post-run read-only analysis materialization
- Published explicit approved measurement execution path:
  - `ReplayHarness.run_with_measurement(...) -> RuntimeMeasurementAdapter.step(...) -> MeasurementBundle.step(...)`
- Documented canonical trace/identity surfaces:
  - `metadata.measurement_traces.{metrics,figures,summary,provenance}`
  - `metadata.measurement_artifact_identity.{hash_algorithm,measurement_payload_hash,preset_name}`
  - report normalization fields `measurement_metrics|measurement_figures|measurement_summary|measurement_provenance`
- Published non-canonical/banned path list and required change-control evidence checklist mapped to V3.22 closeout CI/test gates

### Commit
- `docs(v3.22.20): publish measurement closeout architecture and evidence`

## Slice 5 - Plan Closeout and PR Summary Artifacts
### Scope
- Update:
  - `V3.22.20_plan.md` completion pass
- Add:
  - `docs/V3_PRs/PR_v3_22_summary.md`
- Include final test/CI command set and net-state assertions.

### Commit
- `docs(v3.22.20): finalize v3.22 closeout plan and pr summary`

---

## Testing Gates
- `python -m pytest -q tests/test_v3_measurement_contract_ownership.py tests/test_v3_measurement_bundle_execution.py tests/test_v3_runtime_measurement_adapter.py tests/test_v3_measurement_runtime_parity.py`
- `python -m pytest -q tests/test_v3_measurement_rollout_record_schema.py tests/test_v3_measurement_report_normalization.py tests/test_v3_measurement_trace_compatibility_bridges.py`
- `python -m pytest -q tests/test_v3_measurement_integration_matrix.py`
- `python -m pytest -q tests/test_v3_rollout_replay_harness.py -k "measurement or replay or hash"`

---

## CI Updates
- Add blocking closeout bucket `Run V3.22 measurement closeout gates`.
- Closeout bucket must fail on:
  - measurement ownership/runtime seam drift
  - measurement/report trace drift
  - measurement-inclusive replay/hash non-determinism
  - cross-subsystem integration regressions

---

## Exit Criteria
- Measurement is fully hardened under canonical symbolic + executable + runtime seams.
- Measurement execution remains post-run, single-path, and CI-enforced.
- Measurement traces are stable in rollout and report outputs.
- End-to-end protocol-agent-measurement integration is deterministic and replay-stable.
- Architecture note and evidence checklist provide auditable, repeatable closeout standards.
