# V3.20.10 Plan - Runtime Policy Adapter Seam and Policy Trace Promotion

## Objective
Add canonical runtime policy seam, route runtime selection through executable policy contracts, enforce explicit pre-outcome vs post-outcome causal flow, and promote policy traces into records/report normalization.

---

## Entry Criteria
- V3.20.5 executable policy operators/presets are merged and green.
- Observation and learner runtime seams are stable:
  - `RuntimeObservationAdapter`
  - `RuntimeLearnerAdapter`
- Runtime harness integration points are available for policy seam insertion.
- Typed boundary contracts (`TaskInput`, `Action`, `Outcome`) are available and validated.

### Entry Criteria Status (Start of V3.20.10)
- V3.20.5 executable policy surfaces are present and exported:
  - `virtual_shaping_lab/vsl/agent/policy/operators/base.py`
  - `virtual_shaping_lab/vsl/agent/policy/operators/selection.py`
  - `virtual_shaping_lab/vsl/agent/policy/executable_presets.py`
  - `virtual_shaping_lab/vsl/agent/policy/input.py`
- Runtime seams available for adjacent subsystems:
  - `virtual_shaping_lab/vsl/runtime/observation_adapter.py`
  - `virtual_shaping_lab/vsl/runtime/learner_adapter.py`
- Harness integration seam exists for runtime adapter injection:
  - `CompiledProgramTestEnvironment(..., learner_adapter=..., observation_adapter=...)`
- Typed boundary contracts available:
  - `virtual_shaping_lab/vsl/contracts/interaction.py` (`TaskInput`, `Action`, `Outcome`, `TrialRecord`)

---

## Slices

## Slice 1 - Runtime Policy Adapter
### Scope
- Add:
  - `virtual_shaping_lab/vsl/runtime/policy_adapter.py`
- Implement:
  - `RuntimePolicyAdapter`
  - `build_runtime_policy_adapter(...)`
- Normalize runtime available-actions payloads into canonical policy input transport.

### Implemented (this pass)
- Added canonical runtime policy seam:
  - `virtual_shaping_lab/vsl/runtime/policy_adapter.py`
- Implemented runtime policy APIs:
  - `RuntimePolicyAdapter`
  - `build_runtime_policy_adapter(...)`
- Implemented normalization and typed bridge behavior:
  - available-actions normalization into tuple transport
  - task-input coercion (`TaskInput` or mapping payload)
  - observation-output coercion for typed `build_policy_input(...)` handoff
  - policy execution through executable preset operators only
- Exported runtime policy seam:
  - `virtual_shaping_lab/vsl/runtime/__init__.py`

### Commit
- `feat(v3.20.10): add runtime policy adapter seam`

## Slice 2 - Harness Integration and Ordering
### Scope
- Update runtime harness flow in:
  - `virtual_shaping_lab/vsl/rollout/harness.py`
- Enforce canonical ordering contracts:
  - pre-outcome: `emit -> observe -> predict -> act`
  - post-outcome: `consequence -> learn -> advance`
  - runtime path: observation -> policy -> environment consequence -> learner (action-first tasks)
- Keep ordering protocol-controlled but seam-constrained.
- Explicitly disallow hidden learn-inside-act/step behavior before outcome is available.

### Implemented (this pass)
- Updated runtime harness to route action selection through canonical policy seam:
  - `virtual_shaping_lab/vsl/rollout/harness.py`
- Added runtime policy seam injection point in test environment:
  - `CompiledProgramTestEnvironment(..., policy_adapter=...)`
  - defaults to `build_runtime_policy_adapter()`
- Enforced step ordering in runtime environment flow:
  - pre-outcome: observation + policy action selection
  - post-outcome: reward consequence + learner update
- Preserved explicit caller override semantics:
  - `RolloutHarness.run(..., action=...)` still overrides adapter-selected action when provided
- Added policy trace surface in emitted step metadata:
  - action
  - available actions
  - action scores/probabilities
  - policy metadata/provenance

### Commit
- `refactor(v3.20.10): route runtime action selection through policy seam`

## Slice 3 - Runtime Parity and Ownership Tests
### Scope
- Add:
  - `tests/test_v3_runtime_policy_adapter.py`
  - `tests/test_v3_policy_runtime_parity.py`
  - `tests/test_v3_agent_protocol_loop_contract.py`
- Assert runtime adapter output equals direct executable policy output for equivalent normalized inputs.
- Add seam ownership checks (no phase ad hoc selection fallback in runtime path).

### Implemented (this pass)
- Added runtime policy seam contract tests:
  - `tests/test_v3_runtime_policy_adapter.py`
  - validates runtime adapter construction, action selection path, and available-action normalization
- Added adapter-vs-executable parity test:
  - `tests/test_v3_policy_runtime_parity.py`
  - validates runtime seam output parity against direct executable policy selection
- Added loop ownership and causal-order test:
  - `tests/test_v3_agent_protocol_loop_contract.py`
  - validates pre-outcome policy dispatch and post-outcome learner dispatch ordering
  - validates runtime harness policy seam ownership by import/dispatch surface guards

### Commit
- `test(v3.20.10): add runtime policy parity and seam ownership checks`

## Slice 4 - Record/Report Policy Trace Promotion
### Scope
- Update:
  - `virtual_shaping_lab/vsl/records/adapters/rollout_records.py`
  - `virtual_shaping_lab/analysis/report/report.py`
- Promote policy traces:
  - chosen action
  - available actions
  - action scores/probabilities
  - policy variant/provenance metadata

### Implemented (this pass)
- Promoted policy traces in rollout-record adapter:
  - `virtual_shaping_lab/vsl/records/adapters/rollout_records.py`
  - adds `metadata.policy_traces` from runtime policy metadata:
    - `action`
    - `available_actions`
    - `action_scores`
    - `action_probabilities`
    - `provenance`
- Promoted policy traces in report record normalization:
  - `virtual_shaping_lab/analysis/report/report.py`
  - adds normalized analysis fields:
    - `policy_action`
    - `policy_available_actions`
    - `policy_action_scores`
    - `policy_action_probabilities`
    - `policy_provenance`
  - backfills `action` and `policy_state` from policy traces when absent
- Added slice-level contract assertions:
  - `tests/test_v3_rollout_record_schema.py`
  - `tests/test_report.py`

### Commit
- `feat(v3.20.10): promote policy traces into records and report normalization`

## Slice 5 - CI Runtime Bucket and Gate Tightening
### Scope
- Add blocking CI step:
  - `Run V3.20.10 runtime policy seam and traces`
- Include runtime seam, parity, and observation/learner coupling checks with policy selectors.
- Add guard checks for:
  - no protocol-side prediction-error computation
  - no agent-side protocol mutation
  - no hidden learn-inside-pre-outcome step path

### Commit
- `ci(v3.20.10): enforce runtime policy seam and trace contracts`

---

## Testing Gates
- `python -m pytest -q tests/test_v3_runtime_policy_adapter.py tests/test_v3_policy_runtime_parity.py`
- `python -m pytest -q tests/test_v3_agent_protocol_loop_contract.py`
- `python -m pytest -q tests/test_v3_runtime_observation_adapter.py tests/test_v3_runtime_learner_adapter.py`
- `python -m pytest -q tests/test_v3_rollout_record_schema.py -k "policy"`
- `python -m pytest -q tests/test_report.py -k "policy"`

---

## CI Updates
- Add blocking bucket: `Run V3.20.10 runtime policy seam and traces`.
- Bucket must fail on:
  - runtime seam bypasses
  - parity mismatches between adapter and executable policy path
  - missing policy trace promotion fields in record/report surfaces
  - causal-order violations across emit/act/consequence/learn boundaries

---

## PR and Commit Cadence
- 1 PR for V3.20.10.
- 5 commits (one per slice).
- PR must be green end-to-end; no post-merge seam fixes.

---

## Exit Criteria
- Runtime action selection is adapter-bound to canonical policy executable path.
- No runtime ad hoc policy branch bypasses canonical seam.
- Policy traces are promoted to record/report surfaces with stable fields.
- CI blocks policy seam/trace regressions.
- Runtime preserves two-stage interaction: pre-outcome behavior and post-outcome learning.
